// Deprecated aliases removed — prefer importing `route_names.dart` and using
// `RouteNames.<name>` directly. This file is intentionally left empty as part
// of the route-name canonicalization (Issue #10).

// If anything still imports this file, update the import to `route_names.dart`.
